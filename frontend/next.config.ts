import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['example.com', 'anotherdomain.com'], // Reemplaza con los dominios de tus imágenes
  },
};

export default nextConfig;
